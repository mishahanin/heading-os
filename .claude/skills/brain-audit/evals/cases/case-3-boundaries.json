{
  "id": "case-3-boundaries",
  "description": "The stated boundaries; a model that invents persistence has misread the skill. Checks topic coverage of all three boundaries the question names. NOTE: substring/word checks cannot verify the POLARITY of the answer - a reply that wrongly claims the skill DOES persist would also mention 'persistence'. Read this case as coverage, not as proof of correctness.",
  "input": "Does /brain-audit persist anything, run a daemon, or scan the workspace?",
  "checks": {
    "must_mention": [
      ["persist", "persists", "persistence", "persisting"],
      ["daemon"],
      ["scan", "scans", "scanning"]
    ],
    "must_not_mention": [
      "runs continuously",
      "in the background",
      "writes state"
    ],
    "min_words": 30,
    "hidden_chars_clean": true
  }
}
